export function Summary({
  totalCount,
  query,
}: {
  totalCount: number;
  query: string;
}) {
  return null; /* (
    <Text fw={500}>
      Showing {totalCount} products for {query}
    </Text>
  );*/
}
